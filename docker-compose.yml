services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        # DO NOT EXPOSE PORT ON PRODUCTION ENVIRONMENT
        # ROUTES ARE NOT PROTECTED BY AUTH
        # AND ARE ACCESSIBLE FROM ANY IP ADDRESS
        # USE NGINX OR TRAEFIK INSTEAD
        ports:
            - '${PORT}:${PORT}'
        environment:
            - PORT=${PORT}
            - DB_HOST=postgres
            - DB_PORT=5432
            - DB_USER=postgres
            - DB_PASSWORD=postgres
            - DB_NAME=mailer
            - REDIS_ADDR=redis:6379
            - SMTP_HOST=${SMTP_HOST}
            - SMTP_PORT=${SMTP_PORT}
            - SMTP_USERNAME=${SMTP_USERNAME}
            - SMTP_PASSWORD=${SMTP_PASSWORD}
        depends_on:
            postgres:
                condition: service_healthy
            redis:
                condition: service_healthy
        volumes:
            - ./templates:/app/templates
            - ./attachments:/app/attachments
        networks:
            - mailer-network

    postgres:
        image: postgres:15-alpine
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=mailer
        volumes:
            - postgres_data:/var/lib/postgresql/data
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U postgres']
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - mailer-network

    redis:
        image: redis:7-alpine
        command: redis-server --requirepass ${REDIS_PASSWORD}
        environment:
            - REDIS_PASSWORD=redis
        volumes:
            - redis_data:/data
        healthcheck:
            test: ['CMD', 'redis-cli', '-a', '${REDIS_PASSWORD}', 'ping']
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - mailer-network

    adminer:
        image: adminer:latest
        ports:
            - '8081:8080'
        environment:
            - ADMINER_DEFAULT_DB_DRIVER=pgsql
            - ADMINER_DEFAULT_DB_HOST=postgres
            - ADMINER_DEFAULT_DB_NAME=mailer
            - ADMINER_DESIGN=dracula
        depends_on:
            postgres:
                condition: service_healthy
        networks:
            - mailer-network

networks:
    mailer-network:
        driver: bridge

volumes:
    postgres_data:
    redis_data:
